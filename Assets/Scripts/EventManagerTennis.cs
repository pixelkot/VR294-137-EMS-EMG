using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EventManagerTennis : MonoBehaviour
{
    public GameObject tennisBall;
    public Transform originTennisBall;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (OVRInput.GetDown(OVRInput.Button.SecondaryIndexTrigger) || OVRInput.GetDown(OVRInput.Button.SecondaryIndexTrigger))
        //{
        //    GameObject ball = Instantiate(tennisBall);
        //    ball.transform.position = originTennisBall.transform.position;
        //}
    }

}
